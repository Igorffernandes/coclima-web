import React from 'react';
import PropTypes from 'prop-types';

const Icon = ({
  type,
  active,
}) => {
  return(
    <></>
  )
}

Icon.propTypes = {
  active: PropTypes.bool,
  type: PropTypes.string,
}

export default Icon;